# Test-only deps. Keep production image lean — `pip install -r requirements.txt`
# does not pull these in. CI / local dev runs:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.0
pytest-asyncio>=0.21
aiosqlite>=0.19
fakeredis>=2.20
httpx<0.28          # starlette 0.36 TestClient compat
